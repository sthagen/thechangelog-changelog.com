#!/bin/bash
set -euo pipefail
export OP_SERVICE_ACCOUNT_TOKEN="${OP_SERVICE_ACCOUNT_TOKEN:?must be set}"
export CHANGELOG_RELEASE_TASK=1
fnox exec --profile production -- mix changelog.static.upload
fnox exec --profile production -- mix ecto.migrate
